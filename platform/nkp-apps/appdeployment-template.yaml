apiVersion: v1
kind: ConfigMap
metadata:
  name: kube-prometheus-stack-values
  namespace: workspace-example
  labels:
    app.kubernetes.io/name: kube-prometheus-stack
    app.kubernetes.io/part-of: nkp-platform-apps
data:
  values.yaml: |
    grafana:
      enabled: true
---
apiVersion: apps.kommander.d2iq.io/v1alpha3
kind: AppDeployment
metadata:
  name: kube-prometheus-stack
  namespace: workspace-example
spec:
  appRef:
    kind: ClusterApp
    name: kube-prometheus-stack-46.8.0
  configOverrides:
    name: kube-prometheus-stack-values