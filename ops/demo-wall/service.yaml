apiVersion: v1
kind: Service
metadata:
  name: demo-wall
  namespace: demo-ops
spec:
  type: LoadBalancer
  selector:
    app: demo-wall
  ports:
    - name: http
      port: 80
      targetPort: 8080
