apiVersion: networking.istio.io/v1
kind: DestinationRule
metadata:
  name: catalog-api
  namespace: demo-app
spec:
  host: catalog-api.demo-app.svc.cluster.local
  subsets:
    - name: v1
      labels:
        version: v1
    - name: v2
      labels:
        version: v2

