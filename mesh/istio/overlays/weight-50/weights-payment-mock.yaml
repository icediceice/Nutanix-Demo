apiVersion: networking.istio.io/v1
kind: VirtualService
metadata:
  name: payment-mock
  namespace: demo-app
spec:
  http:
    - route:
        - destination:
            host: payment-mock.demo-app.svc.cluster.local
            subset: v1
          weight: 50
        - destination:
            host: payment-mock.demo-app.svc.cluster.local
            subset: v2
          weight: 50

