apiVersion: networking.istio.io/v1
kind: VirtualService
metadata:
  name: catalog-api
  namespace: demo-app
spec:
  http:
    - route:
        - destination:
            host: catalog-api.demo-app.svc.cluster.local
            subset: v1
          weight: 0
        - destination:
            host: catalog-api.demo-app.svc.cluster.local
            subset: v2
          weight: 100

